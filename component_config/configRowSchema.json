{
    "type": "object",
    "title": "Configuration",
    "required": [
        "table"
    ],
    "properties": {
        "table": {
        "type": "string",
        "title": "Tablename",
        "watch": {
            "tables": "_metadata_.tables"
        },
        "required": true,
        "enumSource": "tables",
        "description": "Choose a table to send to argilla",
        "propertyOrder": 1
        }
    }
}